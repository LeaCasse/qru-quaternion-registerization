# TODO — QRU coherent registerization, next operational pass

## Fixed constraints

- Paper source: `paper/main.tex`.
- Bibliography: `paper/references.bib` if compiling with `latexmk -cd paper/main.tex`.
- Paper figures: `paper/figures/*.png`.
- Generated results: `outputs/figures/*`, `outputs/tables/*`, `outputs/metadata/run_manifest.json`.
- Allowed claims: coherent composability interface, gauge diagnostic, signed Bloch readout, verified finite-register demonstrator.
- Forbidden claims: quantum advantage, quaternion performance advantage, scalable QAE decoder, systematic superiority over $Z$ or Bloch PCA.

---

## P0 — submission blockers

### 0. Clean repository state

Actions:
- Delete transient files:
  - `__pycache__/`
  - `.pytest_cache/`
  - `paper/*.aux`
  - `paper/*.log`
  - `paper/*.out`
  - `paper/*.fdb_latexmk`
  - `paper/*.fls`
  - `paper/*.bbl`
  - `paper/*.blg`
- Update `.gitignore` to block those files.
- Keep only source, tests, scripts, paper figures, generated tables/figures, manifest.

Check:
```bash
find . -type d \( -name "__pycache__" -o -name ".pytest_cache" \)
find paper -maxdepth 1 -type f \( -name "*.aux" -o -name "*.log" -o -name "*.out" -o -name "*.bbl" -o -name "*.blg" \)
```

End:
- Both commands return nothing.

---

### 1. Make `run_all.py` the single reproduction gate

Actions:
- Modify `experiments/run_all.py` so it regenerates:
  - all `outputs/tables/*.csv`
  - all `outputs/tables/*.tex`
  - all `outputs/figures/*.pdf`
  - all `outputs/figures/*.png`
  - all `paper/figures/*.png`
  - `outputs/metadata/run_manifest.json`
- Force the Qiskit resource run to use full validation values, not `NaN` placeholders.
- Keep `_plot_03_outputs.py` as CSV-to-figure only; call it from `run_all.py`.
- Record every executed script and status in `run_manifest.json`.

Commands:
```bash
python experiments/run_all.py
python -m pytest -q
```

End:
- `run_all.py` finishes without manual scripts.
- `paper/main.tex` references only files regenerated by `run_all.py`.

---

### 2. Fix transpilation configuration mismatch

Current issue:
- `paper/main.tex` states optimization level `1`, seed `42`.
- `src/qru_registerization/transpile_config.py` currently uses optimization level `0`, seed `17`.

Actions:
- Choose one final configuration.
- Apply it in:
  - `src/qru_registerization/transpile_config.py`
  - `outputs/metadata/run_manifest.json`
  - `paper/main.tex`
  - resource table caption
- Regenerate:
  - `outputs/tables/03_precision_resource_tradeoff.csv`
  - `outputs/tables/coherent_resource_breakdown.csv`
  - `outputs/tables/coherent_resource_breakdown.tex`
  - `paper/figures/coherent_precision_robustness.png`

Command:
```bash
python experiments/03_coherent_registerization.py
```

End:
- No mismatch between code, manifest, table, caption.
- Resource values in the paper come from regenerated tables.

---

### 3. Remove hard-coded resource table values from `main.tex`

Actions:
- Generate a compact paper table from CSV:
  - input: `outputs/tables/03_precision_resource_tradeoff.csv`
  - input: `outputs/tables/coherent_resource_breakdown.csv`
  - output: `outputs/tables/coherent_resource_paper_table.tex`
- Replace hard-coded table rows in `paper/main.tex` by:
```latex
\input{../outputs/tables/coherent_resource_paper_table.tex}
```
or by a local copy regenerated by `run_all.py`.

Table columns:
- $(m_\phi,m_s)$
- qubits
- depth
- CX
- decoder CX
- grid mean $|\widehat s-s|$
- QRU mean $|\widehat s-s|$

End:
- Changing CSV values changes the paper table after `run_all.py`.
- No resource number is manually typed in `main.tex`.

---

### 4. Final LaTeX/BibTeX gate

Actions:
- Keep `\bibliographystyle{unsrt}` and `\bibliography{references}`.
- Verify that `paper/references.bib` contains all keys used in `main.tex`.
- On Windows/MiKTeX, install a real system Perl executable; do not use `pip install perl`.

Commands:
```bash
latexmk -pdf -cd paper/main.tex
```

Fallback if `latexmk` fails but LaTeX/BibTeX exist:
```bash
cd paper
pdflatex main.tex
bibtex main
pdflatex main.tex
pdflatex main.tex
```

End:
- No citation appears as `?`.
- Bibliography appears in final PDF.
- No missing figure warning.

---

### 5. Final claim audit

Actions:
- Search and edit `paper/main.tex`.

Commands:
```bash
grep -niE "advantage|outperform|better|scalable|Rall|QSP|QSVT|quaternion weighting|algorithm" paper/main.tex
```

Allowed:
- `not scalable`
- `future replacement`
- `QPE-based coherent amplitude estimation`
- `quaternion representation of SU(2) motion`
- `gauge diagnostic`

Forbidden:
- quaternion advantage
- QAE scalability claim
- Rall/QSP/QSVT as implemented contribution
- systematic superiority over Bloch PCA or $Z$

End:
- Every remaining match is either a limitation, a citation context, or future work.

---

### 6. Figure/caption minimality audit

Actions:
- Keep only these paper figures:
  - architecture
  - gauge/projection controls
  - Bloch-register diagnostic
  - readout generalization + precision robustness
- Ensure all paper figures are PNG.
- Ensure every caption contains one factual metric or claim and no mini-introduction.
- Remove any figure panel not used by the surrounding argument.

Check:
```bash
grep -n "\\includegraphics" paper/main.tex
ls paper/figures/*.png
```

End:
- Every included figure exists.
- No figure repeats another figure's argument.

---

### 7. Affiliation gate

Actions:
- Do not change affiliation until confirmed by Bernhard/Albert.
- If confirmed, replace all Waikato affiliations consistently with:
```text
Te Ipu o Te Mahara | Artificial Intelligence Institute, University of Waikato, Hamilton, New Zealand
```

End:
- All Waikato affiliations are identical and author-approved.

---

## P1 — scientific extension only if needed

### 8. Minimal trained-QRU readout experiment

Condition:
- Do this only if the paper keeps any statement about learned-task readout performance.

Actions:
- Implement pure NumPy/SciPy training, no PennyLane dependency.
- Add:
  - `experiments/07_trained_qru_readout.py`
  - optional helper in `src/qru_registerization/training.py`
  - `tests/test_trained_split_protocol.py`
- Train on a teacher state trajectory:
\[
L_{\mathrm{state}}(\theta)=\frac{1}{N}\sum_i
\|\boldsymbol r_\theta(x_i)-\boldsymbol r_{\mathrm{teacher}}(x_i)\|_2^2.
\]
- Split:
  - train: fit model and axes
  - validation: thresholds/selection only
  - test: final metrics only
- Compare:
  - $e_z$
  - best Pauli axis
  - unweighted Bloch PCA
  - state-motion/quotient weighting
  - raw quaternion weighting as ablation only

Outputs:
- `outputs/tables/07_trained_qru_readout.csv`
- `outputs/tables/07_trained_qru_readout_summary.tex`

Metrics:
- $E_{\mathrm{test}}$
- $\mathrm{Err}_{\mathrm{test}}$
- $\Gamma$
- angle with $Z$
- non-identifiable fraction
- paired bootstrap 95% CI

End:
- Either a defensible trained-QRU result is added, or all learned-performance wording is removed.

---

### 9. Trained Bloch-register diagnostic

Condition:
- Only after P1.8.

Actions:
- Reuse `experiments/06_bloch_register_diagnostic.py` with trained parameters.
- Keep checks:
  - identifiability
  - gain vs $Z$
  - projector identity error
  - signed-register error
  - downstream bound
- Do not replace the current random diagnostic if the trained case is non-identifiable or visually worse.

Outputs:
- `outputs/tables/07_trained_bloch_register_summary.csv`
- optional `paper/figures/trained_bloch_register_diagnostic.png`

End:
- Figure included only if it improves the paper's argument.

---

## P2 — future branch, not current paper

### 10. Structured decoder replacement

Keep outside `main` before submission.

Allowed future branch:
- reversible approximation of $2\sin^2(\pi y/2^{m_\phi})-1$
- QSP/QSVT-inspired estimator
- bitwise coherent amplitude estimation

Forbidden in current paper:
- claiming Rall/QSP/QSVT is implemented
- replacing the current finite lookup without full tests/resources

---

### 11. Multi-qubit, mixed-state, channel geometry

Allowed:
- one short discussion sentence if needed.

Forbidden in current paper:
- multi-qubit experiment
- mixed-state metric
- quaternion loss
- channel-distance claim without implementation

---

## Final submission gate

Commands:
```bash
python -m pytest -q
python experiments/run_all.py
latexmk -pdf -cd paper/main.tex
```

Required state:
- all tests pass with Qiskit installed
- all paper figures and tables regenerated
- bibliography resolved
- no transient files left in repo
- no unsupported claim in abstract, introduction, discussion or captions
- if P1.8 is not done, no trained-QRU performance claim remains
